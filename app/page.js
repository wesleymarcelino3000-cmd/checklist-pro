'use client';

import { useCallback, useEffect, useMemo, useRef, useState } from 'react';

const fallbackState={activeList:'geral',lists:{geral:{name:'Checklist Geral',tasks:[{id:'local-1',title:'Revisar atividades do dia',done:false,priority:'high',date:''},{id:'local-2',title:'Conferir itens concluídos',done:true,priority:'normal',date:''}]}}};
const priorityName={high:'Alta prioridade',normal:'Normal',low:'Baixa prioridade'};
const clone=(v)=>JSON.parse(JSON.stringify(v));
const uid=()=>crypto.randomUUID();
function formatDate(v){if(!v)return '';const [y,m,d]=v.split('-');return `${d}/${m}/${y}`;}

export default function Home(){
 const [data,setData]=useState(null),[filter,setFilter]=useState('all'),[search,setSearch]=useState(''),[sync,setSync]=useState('Carregando...'),[newListOpen,setNewListOpen]=useState(false),[newListName,setNewListName]=useState(''),[taskTitle,setTaskTitle]=useState(''),[priority,setPriority]=useState('normal'),[date,setDate]=useState('');
 const firstLoad=useRef(true),saveTimer=useRef(null);
 useEffect(()=>{let cancelled=false;(async()=>{try{const r=await fetch('/api/state',{cache:'no-store'});if(!r.ok)throw new Error();const state=await r.json();if(!cancelled){setData(state);setSync('Sincronizado');firstLoad.current=false}}catch{if(!cancelled){setData(clone(fallbackState));setSync('Modo local');firstLoad.current=false}}})();return()=>{cancelled=true}},[]);
 const persist=useCallback((next)=>{setData(next);if(firstLoad.current)return;setSync('Salvando...');clearTimeout(saveTimer.current);saveTimer.current=setTimeout(async()=>{try{const r=await fetch('/api/state',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(next)});if(!r.ok)throw new Error();setSync('Sincronizado')}catch{setSync('Erro ao sincronizar')}},350)},[]);
 useEffect(()=>()=>clearTimeout(saveTimer.current),[]);
 const active=data?.lists?.[data.activeList],tasks=active?.tasks??[],done=tasks.filter(t=>t.done).length,pending=tasks.length-done,high=tasks.filter(t=>t.priority==='high'&&!t.done).length,progress=tasks.length?Math.round(done/tasks.length*100):0;
 const visibleTasks=useMemo(()=>{const n=search.trim().toLowerCase();return tasks.filter(t=>{const ok=filter==='all'||(filter==='pending'&&!t.done)||(filter==='done'&&t.done)||(filter==='high'&&t.priority==='high');return ok&&t.title.toLowerCase().includes(n)})},[tasks,filter,search]);
 if(!data||!active)return <main className="loading-screen"><div className="loader"/><p>Carregando Checklist Pro...</p></main>;
 function updateActive(fn){const next=clone(data);fn(next.lists[next.activeList],next);persist(next)}
 function addTask(e){e.preventDefault();const title=taskTitle.trim();if(!title)return;updateActive(l=>l.tasks.unshift({id:uid(),title,done:false,priority,date}));setTaskTitle('');setPriority('normal');setDate('')}
 function toggleTask(id){updateActive(l=>{const t=l.tasks.find(x=>x.id===id);if(t)t.done=!t.done})}
 function editTask(task){const title=window.prompt('Editar tarefa:',task.title)?.trim();if(!title)return;updateActive(l=>{const t=l.tasks.find(x=>x.id===task.id);if(t)t.title=title})}
 function deleteTask(id){if(window.confirm('Excluir esta tarefa?'))updateActive(l=>l.tasks=l.tasks.filter(x=>x.id!==id))}
 function clearDone(){if(done&&window.confirm('Remover todas as tarefas concluídas?'))updateActive(l=>l.tasks=l.tasks.filter(t=>!t.done))}
 function renameList(){const name=window.prompt('Novo nome da lista:',active.name)?.trim();if(name)updateActive(l=>l.name=name)}
 function createList(e){e.preventDefault();const name=newListName.trim();if(!name)return;const next=clone(data),id=uid();next.lists[id]={name,tasks:[]};next.activeList=id;persist(next);setNewListName('');setNewListOpen(false);setFilter('all')}
 function selectList(id){const next=clone(data);next.activeList=id;persist(next);setFilter('all');setSearch('')}
 function removeList(){if(Object.keys(data.lists).length===1)return alert('Você precisa manter pelo menos uma lista.');if(!confirm(`Excluir a lista “${active.name}” e todas as tarefas dela?`))return;const next=clone(data);delete next.lists[next.activeList];next.activeList=Object.keys(next.lists)[0];persist(next)}
 const filters=[['all','▦','Todos',tasks.length],['pending','◷','Pendentes',pending],['done','✓','Concluídos',done],['high','!','Alta prioridade',high]];
 return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">✓</div><div><strong>Checklist Pro</strong><span>Organize. Execute. Conclua.</span></div></div><button className="primary-btn full" onClick={()=>setNewListOpen(true)}>+ Nova lista</button><div className="sidebar-label">MINHAS LISTAS</div><div className="list-switcher">{Object.entries(data.lists).map(([id,l])=><button key={id} className={`list-item ${id===data.activeList?'active':''}`} onClick={()=>selectList(id)}><span className="list-dot"/><span>{l.name}</span><b>{l.tasks.length}</b></button>)}</div><div className="sidebar-label filters-label">FILTROS</div><nav className="nav-group">{filters.map(([k,i,l,c])=><button key={k} className={`nav-item ${filter===k?'active':''}`} onClick={()=>setFilter(k)}><span>{i}</span><span>{l}</span><b>{c}</b></button>)}</nav><div className="sidebar-footer"><span className={`sync-dot ${sync==='Sincronizado'?'ok':sync.includes('Erro')?'error':''}`}/>{sync} · Vercel Blob</div></aside><main className="main"><header className="topbar"><div><p className="eyebrow">PAINEL DE CONTROLE</p><h1>Meus checklists</h1><p className="muted">Acompanhe tarefas, prazos e progresso em um só lugar.</p></div><div className="top-actions"><input value={search} onChange={e=>setSearch(e.target.value)} type="search" placeholder="Buscar tarefa..."/><div className="online-pill"><span/> Online</div></div></header><section className="stats"><article><span className="stat-icon">▦</span><div><small>Total</small><strong>{tasks.length}</strong></div></article><article><span className="stat-icon">◷</span><div><small>Pendentes</small><strong>{pending}</strong></div></article><article><span className="stat-icon">✓</span><div><small>Concluídas</small><strong>{done}</strong></div></article><article><span className="stat-icon">↗</span><div><small>Progresso</small><strong>{progress}%</strong></div></article></section><section className="workspace"><div className="workspace-head"><div><h2>{active.name}</h2><p className="muted">{tasks.length} {tasks.length===1?'item':'itens'}</p></div><div className="workspace-actions"><button className="ghost-btn" onClick={renameList}>Renomear</button><button className="ghost-btn danger" onClick={clearDone}>Limpar concluídos</button><button className="ghost-btn danger-subtle" onClick={removeList}>Excluir lista</button></div></div><div className="progress-wrap"><div className="progress-bar"><span style={{width:`${progress}%`}}/></div><small>{progress}% concluído</small></div><form className="task-form" onSubmit={addTask}><input value={taskTitle} onChange={e=>setTaskTitle(e.target.value)} maxLength={120} placeholder="Adicionar uma nova tarefa..." required/><select value={priority} onChange={e=>setPriority(e.target.value)}><option value="normal">Normal</option><option value="high">Alta</option><option value="low">Baixa</option></select><input value={date} onChange={e=>setDate(e.target.value)} type="date"/><button className="primary-btn" type="submit">Adicionar</button></form><div className="task-list">{visibleTasks.map(task=><div className={`task ${task.done?'done':''}`} key={task.id}><button className="check" onClick={()=>toggleTask(task.id)}>{task.done?'✓':''}</button><div><div className="task-title">{task.title}</div><div className="task-meta"><span className={`badge ${task.priority}`}>{priorityName[task.priority]}</span>{task.date&&<span>Prazo: {formatDate(task.date)}</span>}</div></div><div className="task-actions"><button className="mini-btn" onClick={()=>editTask(task)}>✎</button><button className="mini-btn" onClick={()=>deleteTask(task.id)}>✕</button></div></div>)}</div>{!visibleTasks.length&&<div className="empty-state"><div>✓</div><h3>Nada por aqui</h3><p>Adicione uma tarefa ou altere o filtro.</p></div>}</section></main>{newListOpen&&<div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setNewListOpen(false)}><form className="dialog-card" onSubmit={createList}><h3>Nova lista</h3><p className="muted">Crie um espaço separado para outro processo ou rotina.</p><input autoFocus value={newListName} onChange={e=>setNewListName(e.target.value)} placeholder="Ex.: Abertura da loja" maxLength={60} required/><div className="dialog-actions"><button type="button" className="ghost-btn" onClick={()=>setNewListOpen(false)}>Cancelar</button><button className="primary-btn" type="submit">Criar lista</button></div></form></div>}</div>
}
